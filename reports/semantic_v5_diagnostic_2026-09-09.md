# Ramen 语义改进：已测证据与 v5 方案

日期：2026-09-09。旧模型为 v3 导出的 iteration_15000；不能据此推断它就是
验证最优权重或恰好第 15000 次更新。新 v5 与旧权重、教师缓存和输出目录隔离。

## 结论先行

旧系统不仅有模型问题，还存在检索空间与计分协议问题。修正检索后，旧权重在
3 个有标注的 Ramen 测试视角上达到 **mIoU 42.62%**，但筷子和蛋仍明显不足。
这不是重新训练带来的提高。当前不能宣称 v5 已达到论文水平。

### 实测数值

| 测量 | mIoU | Boundary-IoU | 注释 |
|---|---:|---:|---|
| 旧 v3、旧 PCA 检索和旧计分 | 19.2962% | 8.0318% | 保留的历史结果 |
| 同一批旧二值预测，仅改 GG-native 计分 | 19.2962% | 14.0268% | CPU 重计分；预测文件与阈值完全不变 |
| 同一 v3 权重、CLIP 空间相关度、GG-native 计分 | 42.6195% | 33.0646% | 权重未变；检索与计分同时改变，不能把全部差值归因于某个模块 |
| Gaussian Grouping 作者发布的 mask，重算相同 3 个视角 | 76.9512% | 68.6916% | 外部结果参考，不是本机复现；其监督与查询流程不同 |

后三行使用原始 GT 分辨率、GT >128、预测最近邻缩放、边界比例 0.02、补零边缘、
类别宏平均。第一行的边界和图像计分规则不同，不应直接作严格消融。
补充的同预测重计分已在 Colab CPU 完成：仅计分协议把 Boundary-IoU 从 8.0318%
改为 14.0268%，mIoU 不变。统一 GG-native 后，修正检索的增量为 mIoU **+23.3233
个百分点**、Boundary-IoU **+19.0378 个百分点**；这属于检索实现修复，不是重新训练。
该诊断未重新挑选阈值或改变已存二值预测，GT 指纹与作者 mask 重测相同。
上述旧权重的 3 个标注视角 RGB 为 PSNR **29.9709 dB**、SSIM **0.931923**；
全部 4 个 RGB 测试视角为 **30.2791 dB / 0.934865**。两组视角不可混用。

| 类别 | v3 修正检索后的 IoU | 作者发布 GG mask 重算 IoU |
|---|---:|---:|
| chopsticks | 6.73% | 82.49% |
| egg | 9.42% | 90.58% |
| glass of water | 77.92% | 86.60% |
| pork belly | 30.86% | 94.41% |
| wavy noodles in bowl | 59.77% | 16.63% |
| yellow bowl | 71.02% | 91.00% |

GG 的 noodles 在这几个固定视角很低，不能只选择对自己有利的类别报告。
外部模型没有在此次运行中计时，也没有用这些 mask 推断它的 PSNR。

## 二维教师诊断

固定 SAM ViT-H、OpenCLIP ViT-H-14，192 个保留区域，语义宽 512。
以下在 3 张测试图做前向诊断，不用测试图拟合 PCA/原型或更新模型。
诊断 PCA 采用旧训练元数据，不是新 v5 的独立训练集 PCA。

| SAM 裁剪层数 | 保留候选单 mask oracle IoU | 原始 CLIP 自动查询 union IoU | 原始 CLIP 自动查询互斥 region-map IoU |
|---|---:|---:|---:|
| 0 | 85.8285% | 48.6810% | 36.6636% |
| 1 | 89.2741% | 47.4937% | 34.2545% |

“oracle”用人工 GT 从候选中挑最佳单 mask，只诊断候选轮廓上限，不是自动算法结果。
增加裁剪提高了候选上限，却没有提高此次自动语言选择指标。因此不能只靠更多 SAM
mask 或更多高斯解决语义问题。原始多视角 CLIP 描述、区域层级与独立亲和分支是新方案重点。

## v5 组合方法

1. **几何预热**：完整实验先做 2500 次 RGB 更新，再用 2000 次余弦渐增引入语义；
   最多 15000 次，12 个验证视角每 1000 次检查。预热是可配置超参数，当前不是已完成搜索的最优值。
2. **拆开“名称”和“同一物体”**：32 维语言字段蒸馏训练集 PCA 后的 CLIP；16 维独立有符号亲和字段
   学习 SAM 同组/异组关系。不能用实例负对比把同类物体的语言表示强行推远。
3. **粗—中—细层级**：真实二维 mask 包含关系（包含率 0.9、面积增长 1.15），8/12/16 维亲和前缀；
   冲突层级忽略，不把未知区域当负样本。不是 SAGA 的物理三维连续尺度。
4. **边界与细长物体**：完整裁剪补成方形避免切掉两端；区域面积平衡取平方根、上限 8；
   边界/细长区域加强细节权重，但不自动升为重要类别。默认语义不反传几何，RGB 仍正常优化几何。
5. **训练后多视角区域描述符库**：仅训练视角，保留原始完整 CLIP、重叠 mask、粒度、视角和可靠性；
   用独立亲和建立跨视角支持，至少两个不同视角。文本找到描述符，再映射三维点/渲染像素；无支持返回空。
   不是 LaGa 的完整对象级分解与 adaptive K-means 复现。
6. **三级重要性**：显式重要/普通/背景词表竞争，语义证据不足保普通且不更新持久重要性；
   分数升降均用 0.9 EMA。RGB 权重 0.75/1/2，语义权重 0.5/1/2，SH 为 1/2/3；
   保留原始 clone/split/prune，分裂阈值按 1.15/0.8/0.5 调整。

描述符库默认 24 个训练视角、每视角最多 96 区域、每区域最多 512 像素；文本相关度阈值 .5、
点亲和阈值 .7、跨视角亲和 .8、至少 2 视角、最多 64 候选，最终加权分数阈值 .25。
这些是预先设定的初始值，不根据测试标注调参。主基线比较仍使用相同 CLIP 检索规则；
描述符库另存独立评测目录，不能静默混用协议。

## 首次真实 CUDA 训练检查

提交 `3d7faad` 在 Colab L4 通过 **173 项完整代码测试**（1.530 秒），随后真正
执行了新教师预处理和 300 步训练，不使用模拟渲染器。

- 独立子集：6 个训练视角、2 个验证视角、4 个官方测试相机；PCA 只拟合 6 个训练视角。
- 几何初始化来自保留的旧 RGB 7000 权重，1,314,170 个高斯；本次禁止致密化，
  因此这个数量是初始化带入，不是 300 步新增的点数。
- 32 维语言、16 维亲和字段导出成功且均为有限数；步骤 100/200/300 两分支梯度均非零且有限。
- 这些步骤的亲和损失分别为 0.4210 / 0.2043 / 0.1932；随机视角不同，不能把三点当严格学习曲线。
- 成功构建 4 个训练视角、321 条描述符的区域库，261,065 字节；SHA256：
  `4a1dabe0ee99d51b4d51433eccf5de13a6a3cc543cff37448130958e0e1f3e38`。
- CPU 文本查询 `egg` 也实际跑通，返回 35,715 个候选高斯（总数 1,314,170）；
  候选数量不是精度证据。Drive 小型教师快照实际返回 `snapshot_complete`，30 个文件，
  此检查不包含大模型 checkpoint 上传。
- 预处理阶段约 130 秒；训练+加载+验证+导出约 130 秒，训练进度条本身约 80 秒。
  轮询每 10 秒，阶段时间包含轮询间隔，不能当精确训练速度。

两种查询都已完成 3 个标注视角的实际渲染与评估：

| 300 步同一组权重 | mIoU | Boundary-IoU |
|---|---:|---:|
| CLIP 相关度，阈值 .5 | 22.3069% | 19.2677% |
| 区域描述符库，最终分数阈值 .25 | 23.8397% | 16.9434% |

两者 RGB 相同：PSNR 26.1710 dB、SSIM 0.851979。重要/普通/背景高斯分别
72,160 / 1,138,401 / 103,609。描述符库在这次短测提高了区域 IoU、降低了边界 IoU，
因此不能声称已全面改进；还要看正式训练。**当前短测没有达到成熟模型的质量要求。**

这是运行正确性检查，不是达到 15K 精度的模型。少量视图、旧几何初始化、未收敛语义
及分档 SH 调整都不适合与旧完整训练数值直接比较。

## 测试边界与文件

- Colab L4 上，提交 `af697c3` 的 **141 项测试全部通过**。这些含 CPU 张量/模拟渲染器测试，
  不能代替新模型真实 CUDA 训练；旧权重评估与 SAM/CLIP 推理已用真实 GPU 完成。
- 新增重要性与描述符库已纳入上面的 173 项 Colab 测试；本地无 PyTorch，不能把本地跳过项称通过。
- 后续提交 `0922450` 的备份与 notebook 检查加入后，Colab 全套 **195 项测试全部通过**（4.379 秒）。
- 300 步检查从保留的 SH3 RGB 权重初始化，只验证训练、梯度、导出、构库和查询。
  它不是 300 步从零训练的精度报告，也不是独立稀疏视角重建：子集使用原全场景 COLMAP 几何与相机位姿。
- 正式质量结论需要完整 v5 与顺序基线共同完成、冻结同一数据划分和计分方式；15000 步结果尚不能预报。

已保存云端证据：`MyDrive/semantic_adaptive_3dgs/ramen_semantic_audit_2026_09_09`，
118,056,953 字节，包含诊断 JSON、原始评测、逐类别图片和测试日志。
旧训练模型没有因此次实验被覆盖。

300 步检查的日志、梯度诊断、描述符库和两种评测图已另存同目录的 `smoke_300`，
共 53,173,735 字节，经复制后读回 SHA 校验。不含大体积 smoke 权重，云盘空间优先用于正式训练。
完整 131 个候选视角的教师预处理已开始：119 个训练、12 个验证；最多 15000 步的
正式训练及其等时间基线尚未完成，不用短测数字代替正式结果。

主要结果 SHA256：

- v3 修正检索：`e26e057167839308695695767e241838cc50fc8ad34c80beaee5c09932c1edb7`
- GG 同视角：`b59c3a3aeda630ca4a0bbbefddde7ef781d9064a2e8a2ed2682230e5a939f9c9`
- GG 官方 mask 压缩包：`537cd7dfd9f17e6b2429d0c992d53c66c97706dda047d36645f0537fe8abc648`
- 旧二值 mask 统一计分：`d0e726ead018e72c6d7d1fbdbda51a424766dde1ea6757c2bfc812d76ffc7916`

## 来源

- [LaGa 官方代码](https://github.com/SJTU-DeepVisionLab/LaGa)：亲和表示、视角相关语言描述与对象级处理的借鉴来源。
- [SAGA 官方代码](https://github.com/Jumpat/SegAnyGAussians)：多尺度亲和与区域对比的借鉴来源。
- [LangSplat 官方代码](https://github.com/minghanqin/LangSplat)：区域语言蒸馏、语言场与检索的借鉴来源。
- [Gaussian Grouping 官方数据说明](https://github.com/lkeab/gaussian-grouping/blob/main/docs/dataset.md)及
  [作者发布预测](https://huggingface.co/mqye/Gaussian-Grouping/blob/main/result/lerf_mask.zip)：外部参考结果来源。

完整实现与局限见 [SEMANTIC_V5.md](../docs/SEMANTIC_V5.md)。
